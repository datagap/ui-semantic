# UI Semantic tslint config

Create a `tslint.json` file at the root of your project with the following content:

```json
{
  "extends": ["./node_modules/@dgx/coding-standards/tslint/tslint.js"]
}
```
